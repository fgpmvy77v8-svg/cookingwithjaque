Put photos here.

- `jaque.jpg` — About-section portrait, roughly 4:5 (e.g. 1000×1250).
- Dish photos — roughly 4:3 (e.g. 1200×900), then reference the filename with
  `img: 'yourfile.jpg'` on that dish in `assets/main.js`.

Missing files fall back to the illustrated gradient cards automatically.
